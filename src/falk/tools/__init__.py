"""Tool implementations (BSL + DuckDB).

Modules
-------
warehouse.py     — BSL-backed query execution (metrics, dimensions, filters)
semantic.py      — Semantic-model info lookups (from BSL models)
calculations.py  — Analytics helpers (shares, period comparison, date ranges)
"""
