# ─── LLM Provider (choose one) ──────────────────────────────────────────
# Set the model and corresponding API key for your chosen provider:
# For a complete list of supported models, see: https://ai.pydantic.dev/api/models/base/

# OpenAI (default)
# LLM_MODEL=gpt-4o-mini  # or gpt-4o, gpt-4-turbo, etc.
# OPENAI_API_KEY=sk-...

# Anthropic Claude
# LLM_MODEL=anthropic:claude-3-5-sonnet-20241022  # or claude-3-opus, claude-3-haiku
# ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini
# LLM_MODEL=google-genai:gemini-1.5-pro  # or gemini-1.5-flash, gemini-pro
# GOOGLE_API_KEY=...

# ─── Optional: paths ────────────────────────────────────────────────────
# BSL_MODELS_PATH=config/semantic_models.yaml

# ─── Slack integration (Socket Mode) ─────────────────────────────────
# SLACK_BOT_TOKEN=xoxb-...   # OAuth & Permissions → Install App → Bot User OAuth Token
# SLACK_APP_TOKEN=xapp-...   # Basic Information → App-Level Tokens (connections:write scope)

# ─── LangFuse observability (optional) ─────────────────────────────────
# Get keys from https://cloud.langfuse.com or your self-hosted instance
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_HOST=https://cloud.langfuse.com  # or your self-hosted URL
