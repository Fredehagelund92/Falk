# ─── API keys (required for your provider) ───────────────────────────────
# Provider and model are set in falk_project.yaml (agent.provider, agent.model).

# OpenAI (default in scaffold)
# OPENAI_API_KEY=sk-...

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini
# GOOGLE_API_KEY=...

# ─── Session storage (PostgreSQL) ─────────────────────────────────────────
# Required for production. Falk creates schema and tables automatically.
# POSTGRES_URL=postgresql://user:pass@localhost:5432/falk

# ─── Slack integration (Socket Mode) ─────────────────────────────────
# SLACK_BOT_TOKEN=xoxb-...   # OAuth & Permissions → Install App → Bot User OAuth Token
# SLACK_APP_TOKEN=xapp-...   # Basic Information → App-Level Tokens (connections:write scope)

# ─── Logfire observability (optional) ─────────────────────────────────
# Option A (dev): uv run logfire auth && uv run logfire projects use <project-name>
# Option B (prod): LOGFIRE_TOKEN=...  # or LOGTAIL_TOKEN for legacy

# ─── Long-term memory (optional, Hindsight API) ───────────────────────────
# HINDSIGHT_API_URL=http://localhost:8888
# HINDSIGHT_API_KEY=...
